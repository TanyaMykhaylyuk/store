# Store

Initial project setup for a shop.

- `apps/web` — Next.js, React, TypeScript
- `apps/api` — Express, Node, TypeScript
- `packages/types` — shared TypeScript types

```bash
npm install
npm run dev
```

- Web: http://localhost:3000
- API: http://localhost:4000/api/health
